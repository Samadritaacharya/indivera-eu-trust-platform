(()=>{
const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
const prefers=matchMedia('(prefers-reduced-motion: reduce)');let motionOff=prefers.matches||localStorage.getItem('indivera-motion')==='off';
function setMotion(off){motionOff=!!off;document.documentElement.dataset.motion=motionOff?'off':'on';localStorage.setItem('indivera-motion',motionOff?'off':'on');const b=$('#ivMotion');if(b){b.setAttribute('aria-pressed',String(motionOff));b.textContent=motionOff?'Still':'Motion';}}
function el(tag,cls,text){const n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=String(text);return n;}
function injectChrome(){document.body.classList.add('immersive');const prog=el('div','iv-progress');prog.append(el('span'));document.body.prepend(prog);const canvas=el('canvas','iv-canvas');canvas.setAttribute('aria-hidden','true');document.body.prepend(canvas);const glow=el('div','iv-cursor');glow.setAttribute('aria-hidden','true');document.body.prepend(glow);
 const top=$('.topbar');if(top){const btn=el('button','iv-motion',motionOff?'Still':'Motion');btn.id='ivMotion';btn.type='button';btn.setAttribute('aria-pressed',String(motionOff));btn.title='Toggle interface motion';btn.addEventListener('click',()=>{setMotion(!motionOff);location.reload();});top.append(btn);} const dots=el('aside','iv-dots');dots.setAttribute('aria-label','Page sections');[['top','Home'],['ready','Ready'],['verified','Verified'],['match','Match'],['passport','Passport'],['trade','Trade'],['trust','Trust']].forEach(([id,label])=>{const a=el('a');a.href=`#${id}`;a.dataset.dot=id;a.append(el('span','',label));dots.append(a);});document.body.append(dots);}
function injectStory(){const metrics=$('.metrics');if(!metrics||$('#ivStory'))return;const s=el('section','iv-story shell reveal');s.id='ivStory';const intro=el('div','iv-story-intro');intro.append(el('p','eyebrow','THE GAP INDIVERA CLOSES'),el('h2','', 'Finding a supplier is easy. Trusting the evidence is not.'),el('p','iv-story-copy','IndiVera sits between discovery and procurement, turning fragmented European requirements into a visible trust workflow.'));
 const cards=el('div','iv-story-cards');[['01','INDIAN MANUFACTURER','What does a German buyer actually need from me?','Product data, evidence, buyer expectations and regulatory requirements are scattered across different sources.'],['02','INDIVERA','Normalize. Check. Explain. Prove.','Rules determine findings. AI can structure documents and explain gaps, but cannot certify compliance or override evidence.'],['03','EUROPEAN BUYER','Show me suppliers who already did the homework.','Comparable supplier evidence, explainable fit and traceability appear before samples or procurement.']].forEach(([n,k,t,b])=>{const c=el('article','iv-story-card reveal');c.append(el('span','iv-num',n));const d=el('div');d.append(el('p','iv-kicker',k),el('h3','',t),el('small','',b));c.append(d);cards.append(c);});s.append(intro,cards);metrics.after(s);}
function enhanceHero(){const hero=$('.hero');if(!hero)return;hero.classList.add('iv-hero');const first=hero.firstElementChild;if(first){first.classList.add('reveal');const proof=el('div','iv-proof');[['Rules before models','Deterministic readiness logic'],['Evidence before claims','Supplier proof stays explicit'],['Human before transaction','No autonomous legal decisions']].forEach(([a,b])=>{const d=el('div');d.append(el('b','',a),el('span','',b));proof.append(d);});first.append(proof);}const card=$('.hero-card');if(card){card.classList.add('iv-console','tilt-card','reveal');const head=el('div','iv-console-head');head.append(el('span','iv-live','LIVE TRUST PIPELINE'),el('small','iv-clock','—'));card.prepend(head);$$('.suite-step',card).forEach((step,i)=>{step.classList.add('iv-suite-step');step.tabIndex=0;const ids=['ready','verified','match','passport','trade'];const go=()=>document.getElementById(ids[i])?.scrollIntoView({behavior:motionOff?'auto':'smooth'});step.addEventListener('click',go);step.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' ')go();});});const foot=el('div','iv-console-foot');foot.append(el('span','', 'GPSR blocker detected'),el('b','', 'EU responsible person missing'));card.append(foot);}const actions=$('.hero-actions');$$('.button',actions||document).forEach(b=>b.classList.add('magnetic'));}
function enhanceSections(){['ready','verified','match','passport','trade','trust'].forEach((id,i)=>{const sec=document.getElementById(id);if(!sec)return;sec.classList.add('iv-section');sec.style.setProperty('--section-no',`"0${i+1}"`);const h=$('.section-heading',sec);h?.classList.add('reveal');$$('.panel',sec).forEach(p=>p.classList.add('iv-panel'));});const result=$('#scanResult');result?.classList.add('iv-sticky');const match=$('#matchResults');if(match){const viz=el('div','iv-match-viz');viz.setAttribute('aria-hidden','true');viz.append(el('span','iv-buyer','BUYER'));for(let i=0;i<3;i++)viz.append(el('i'));for(let i=0;i<3;i++)viz.append(el('b',`iv-supplier s${i+1}`));match.prepend(viz);}const p=$('#passportResult');if(p)p.classList.add('iv-passport');const trade=$('.timeline');trade?.classList.add('iv-timeline');}
function reveal(){const items=$$('.reveal, .iv-panel, .metrics');if(motionOff){items.forEach(x=>x.classList.add('is-visible'));return;}const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target);}}),{threshold:.12,rootMargin:'0px 0px -7%'});items.forEach(x=>io.observe(x));}
function scrollUi(){const bar=$('.iv-progress span'),top=$('.topbar'),sections=['ready','verified','match','passport','trade','trust'];const tick=()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.transform=`scaleX(${max?scrollY/max:0})`;top?.classList.toggle('iv-scrolled',scrollY>18);};tick();addEventListener('scroll',tick,{passive:true});const io=new IntersectionObserver(es=>{const hit=es.filter(x=>x.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!hit)return;$$('[data-dot]').forEach(a=>a.classList.toggle('active',a.dataset.dot===hit.target.id));},{threshold:[.2,.45],rootMargin:'-18% 0px -58%'});sections.forEach(id=>{const n=document.getElementById(id);if(n)io.observe(n);});}
function motion(){if(motionOff||matchMedia('(pointer:coarse)').matches)return;const orb=$('.iv-cursor');let x=innerWidth/2,y=innerHeight/2,tx=x,ty=y;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY;},{passive:true});(function loop(){x+=(tx-x)*.1;y+=(ty-y)*.1;orb.style.left=`${x}px`;orb.style.top=`${y}px`;requestAnimationFrame(loop);})();$$('.tilt-card').forEach(c=>{c.addEventListener('pointermove',e=>{const r=c.getBoundingClientRect(),px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;c.style.transform=`rotateX(${(-py*4).toFixed(2)}deg) rotateY(${(px*5).toFixed(2)}deg) translateY(-2px)`;});c.addEventListener('pointerleave',()=>c.style.transform='');});$$('.magnetic').forEach(b=>{b.addEventListener('pointermove',e=>{const r=b.getBoundingClientRect();b.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.05}px,${(e.clientY-r.top-r.height/2)*.08}px) translateY(-2px)`;});b.addEventListener('pointerleave',()=>b.style.transform='');});}
function canvas(){const c=$('.iv-canvas');if(!c||motionOff)return;const ctx=c.getContext('2d');let w,h,dpr;const pts=Array.from({length:28},()=>({x:Math.random(),y:Math.random(),vx:(Math.random()-.5)*.00007,vy:(Math.random()-.5)*.00005,r:.5+Math.random()}));const size=()=>{dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)};size();addEventListener('resize',size,{passive:true});(function draw(){ctx.clearRect(0,0,w,h);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0)p.x=1;if(p.x>1)p.x=0;if(p.y<0)p.y=1;if(p.y>1)p.y=0;ctx.beginPath();ctx.arc(p.x*w,p.y*h,p.r,0,Math.PI*2);ctx.fillStyle='rgba(220,210,197,.10)';ctx.fill();}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],dx=(a.x-b.x)*w,dy=(a.y-b.y)*h,d=Math.hypot(dx,dy);if(d<150){ctx.beginPath();ctx.moveTo(a.x*w,a.y*h);ctx.lineTo(b.x*w,b.y*h);ctx.strokeStyle=`rgba(100,120,180,${(1-d/150)*.045})`;ctx.stroke();}}requestAnimationFrame(draw);})();}
function liveClock(){const t=$('.iv-clock');if(!t)return;const f=()=>t.textContent=new Intl.DateTimeFormat('en-GB',{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date());f();setInterval(f,1000);}
function watchDynamic(){const enhance=()=>{$$('#scanResult .finding, #matchResults .match-card, #supplierList .verified-card, #tradeList .list-item').forEach((x,i)=>{x.classList.add('iv-result');x.style.setProperty('--delay',`${Math.min(i*45,240)}ms`);});};enhance();const mo=new MutationObserver(enhance);['scanResult','matchResults','supplierList','tradeList'].forEach(id=>{const n=document.getElementById(id);if(n)mo.observe(n,{childList:true,subtree:true});});}
injectChrome();injectStory();enhanceHero();enhanceSections();setMotion(motionOff);reveal();scrollUi();motion();canvas();liveClock();watchDynamic();
})();
